name: snow_login
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  dio: ^4.0.0
  flutter_polyline_points: ^2.0.0


  cupertino_icons: ^1.0.2

  google_maps_flutter: ^2.0.12
  flutter_svg: ^2.0.10+1  # Updated to a version that supports null safety

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/model.h5
    - assets/model.tflite

